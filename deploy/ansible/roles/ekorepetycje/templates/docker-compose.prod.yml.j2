# Production overrides — generated by Ansible
services:
  db:
    restart: always

  web:
    restart: always
    command: --port {{ app_port }} --workers {{ app_workers }}
    volumes: []   # override dev bind-mounts — use image layers only

volumes:
  postgres_data:
    external: true
